export function App() {
  return (
    <main className="shell">
      <p className="eyebrow">LAN-first runner fleet</p>
      <h1>Tewake</h1>
      <p className="lede">
        Turn trusted computers you already own into a private GitHub Actions fleet.
      </p>
      <section aria-labelledby="status-title" className="card">
        <h2 id="status-title">Development status</h2>
        <p>The controller and agent contracts are being implemented from the Tewake spec.</p>
      </section>
    </main>
  );
}
