{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "sparerunner",
  "title": "SpareRunner",
  "description": "See what this computer is running for your SpareRunner fleet, and stop or resume accepting new jobs.",
  "icon": "command-icon.png",
  "author": "genm",
  "license": "Apache-2.0",
  "categories": ["Developer Tools"],
  "commands": [
    {
      "name": "status",
      "title": "Node Status",
      "subtitle": "SpareRunner",
      "description": "Show whether this computer accepts new fleet jobs and what it is running.",
      "mode": "view"
    },
    {
      "name": "pause",
      "title": "Stop Accepting Jobs",
      "subtitle": "SpareRunner",
      "description": "Withhold this computer's capacity without cancelling the job already running.",
      "mode": "no-view"
    },
    {
      "name": "resume",
      "title": "Resume Accepting Jobs",
      "subtitle": "SpareRunner",
      "description": "Offer this computer's capacity again once the controller confirms it.",
      "mode": "no-view"
    }
  ],
  "preferences": [
    {
      "name": "cliPath",
      "type": "textfield",
      "required": false,
      "title": "SpareRunner CLI",
      "description": "Path to the sprun binary. Leave empty to search the standard install locations.",
      "placeholder": "/usr/local/bin/sprun"
    },
    {
      "name": "stateDirectory",
      "type": "textfield",
      "required": false,
      "title": "Agent State Directory",
      "description": "Override the agent state directory when it is not the OS default.",
      "placeholder": "~/Library/Application Support/sparerunner/agent"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.100.0",
    "@raycast/utils": "^2.2.7"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "brace-expansion": "^5.0.8"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "ray lint",
    "typecheck": "tsc --noEmit"
  }
}
